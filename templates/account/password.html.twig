{% extends 'base.html.twig' %}

{% block body %}
   <div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border rounded  bg-light">
                <div class="card-header text-center">
                    <h4>Modifier mon mot de passe</h4>
                <div class="card-body">
                    {{ form_start(modifypwd) }}
                        {{ form_widget(modifypwd) }}
                    {{ form_end(modifypwd) }}
                </div>
            </div>
        </div>
    </div>
</div>
    <div class="container my-5">
        <h1>Espace membre privé</h1>
        <h3>Modifier mon mot de passe</h3>
      
1.FORMULAIRE
form
    1.a. Mode de passe actuel 2. Mot de passe actuel->compare le mot de passe actuel avec le mot de passe enregistré dans la base de données
    1.b. Nouveau mot de passe->compare le nouveau mot de passe avec le mot de passe enregistré dans la base de données
    1.c. Confirmation du nouveau mot de passe->compare la confirmation du nouveau mot de passe avec le nouveau mot de passe
    
    2. Mise à jour de la base de données
    2.a. lui envoyer une petite notification pour lui dire que son mot de passe a été mis à jour
    </div>

{% endblock %}
